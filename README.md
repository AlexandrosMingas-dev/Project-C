The algorithm treats the event as a series of discrete time slots (e.g., 18:00, 19:00, 20:00). 
It calculates the how many bands are present at each specific hour and then identifies the busiest (or quietest) continuous interval.
